<MessageCollection>

<!-- Plugin information -->
    <Plugin>
        <ShortDescription>MutabilityDetectorFindbugs plugin</ShortDescription>
        <Details>
            <![CDATA[
<p>This plugin detects classes annotated with @Immutable that are actually mutable</p>
]]>
        </Details>
    </Plugin>

  <Detector class="org.mutabilitydetector.findbugs.ThisPluginDetector" >
    <Details>
      <![CDATA[<p> This plugin detects classes annotated with @Immutable that are actually mutable.
</p>]]>
    </Details>
  </Detector>


  <BugPattern type="JCIP_IS_NOT_IMMUTABLE">
    <ShortDescription>@Immutable class is actually mutable</ShortDescription>

<LongDescription>{1} is annotated as Immutable, but is actually mutable.</LongDescription>

<Details>
  <![CDATA[

]]>
</Details>
  </BugPattern>

  <BugCode abbrev="JCIP">@Immutable class is actually mutable</BugCode>
  
</MessageCollection>

